# 蓝牙打印机连接故障排除指南 (2026-03-14)

## 问题分析

从您的截图中，蓝牙连接失败，错误信息不完整（显示为 `Error {}`）。这表示错误对象的 `message` 属性为空或未定义。

### 可能的原因：

| 错误类型 | 症状 | 解决方案 |
|---------|------|--------|
| **NotAllowedError** | 点击蓝牙设备选择框但看不到设备，或弹窗立即关闭 | 检查浏览器权限，尝试用 Chrome/Edge 重新授权蓝牙 |
| **NotFoundError** | 搜索时找不到打印机 | 确保打印机已开机、有电、在蓝牙范围内（5m以内） |
| **SecurityError** | HTTP 环境下使用蓝牙 | 需要 HTTPS 或 localhost，HTTP 不支持蓝牙 API |
| **GATT 连接失败** | 找到设备但无法连接 | 打印机可能需要重启或忘记重新配对 |
| **特征不可用** | 连接成功但找不到可写特征 | 该设备可能不兼容或支持不同的服务 UUID |

---

## 已修复的改进

### 1. **更完整的错误日志** ✅
现在错误信息将显示：
- `Name: NotAllowedError` - 错误类型
- `Message: 具体错误描述` - 错误信息
- `Code: 错误代码` - 如果有
- 完整的错误对象内容

### 2. **检查 HTTPS/localhost 环境** ✅
```javascript
const isSecureContext = window.isSecureContext;
if (!isSecureContext) {
  throw new Error('需要 HTTPS 安全连接或 localhost 才能使用蓝牙功能');
}
```
- 如果在 HTTP 环境使用，会提前收到清晰的错误提示

### 3. **支持更多的打印机型号** ✅
**扩展的蓝牙设备过滤：**
- XP 系列（XP-58, XP-P300 等）
- MTP 系列
- P300, P- 开头
- 通用 Printer, POS, ESC 等

### 4. **多个服务 UUID 尝试** ✅
现在支持 3 个常见的蓝牙服务：
- `000018f0...` - 标准 SPPS (Serial Port Profile Service)
- `0000ffe0...` - 通用数据传输（许多打印机使用）
- `0000fff0...` - 另一个常见的 UUID

代码会自动尝试每个 UUID，并报告哪些可用、哪些不可用。

### 5. **改进的用户错误提示** ✅
现在会根据错误类型显示友好的中文提示：
- 取消设备选择 → "您取消了蓝牙设备的选择"
- 未找到设备 → "未找到打印机。请确保打印机已开机并在蓝牙范围内"
- HTTPS 问题 → "需要 HTTPS 安全连接才能使用蓝牙功能"
- 连接失败 → "蓝牙 GATT 连接失败（设备可能已断开或不支持）"

---

## 打印机连接步骤和排查

### ✅ 基本检查清单

```
□ 1. 确认打印机已开机
     - 查看打印机显示屏或 LED 灯
     - 听听是否有开机声音

□ 2. 确认浏览器支持蓝牙
     - Chrome 79+ 或 Edge 79+（其他浏览器不支持蓝牙 API）
     - Safari 不支持，Firefox 不支持

□ 3. 确认网站使用 HTTPS 或 localhost
     - 地址栏应该显示 🔒 或 localhost:
     - http:// 不支持蓝牙

□ 4. 检查蓝牙范围
     - 打印机距离设备应在 5 米内
     - 避免金属物、微波炉等干扰源

□ 5. 检查系统蓝牙设置
    Windows 11:
    - 设置 > 蓝牙和设备 > 蓝牙
    - 应该看到打印机在"已连接"或"已配对"列表中
    - 如果没有，需要手动配对（打印机蓝牙要开启搜索模式）

    macOS:
    - 系统偏好设置 > 蓝牙
    - 应该看到打印机已配对

□ 6. 检查浏览器权限
    - 浏览器应该提示 "website wants to access Bluetooth"
    - 点击 "允许"
    - 如果看不到提示，浏览器可能被锁定了蓝牙权限
```

---

## 调试技巧 - 查看控制台日志

### 打开开发者工具

**Windows/Linux:**
- 按 `F12` 或 `Ctrl + Shift + I`

**macOS:**
- 按 `Cmd + Option + I`

### 查看关键日志

在控制台（Console 标签）中，查找这些日志信息：

```javascript
// ✅ 正常流程应该看到这些日志
[Bluetooth] 正在搜索打印机...           // 1. 点击蓝牙打印后
[Bluetooth] 正在请求蓝牙设备...        // 2. 即将弹出设备选择框
[Bluetooth] 找到设备: XP-P300           // 3. 用户选择了设备
[Bluetooth] 已连接到 GATT 服务器       // 4. GATT 连接成功
[Bluetooth] 尝试获取服务 000018f0...   // 5. 尝试第一个服务
[Bluetooth] ✅ 成功获取服务 000018f0... // 6. 找到了可用的服务
[Bluetooth] 找到 2 个特征              // 7. 找到可写特征
[Bluetooth] 打印机已就绪               // 8. 准备打印

// ❌ 错误情况会显示这些
[Bluetooth] 连接失败: Name: NotFoundError // 未找到设备
[Bluetooth] 连接失败: Name: NotAllowedError // 用户取消
[Bluetooth] 连接失败: Name: SecurityError // 需要 HTTPS
```

---

## 常见问题 & 解决方案

### Q1: 看到设备选择框但列表为空

**原因：** 
- 打印机搜索模式未开启
- 系统蓝牙中打印机未配对

**解决：**
1. 打印机需要进入蓝牙搜索模式（通常是长按电源键 3 秒）
2. 打开 Windows 蓝牙设置，手动搜索并配对打印机
3. 重新尝试网页上的蓝牙连接

### Q2: 点击蓝牙打印后没有任何反应

**原因：** 
- 浏览器不支持蓝牙（Safari、Firefox）
- 网站不是 HTTPS/localhost
- 浏览器权限被禁用

**解决：**
1. 确认使用 Chrome 或 Edge 最新版本
2. 确认地址栏显示 🔒（HTTPS）或 localhost
3. 检查浏览器权限：Settings > Privacy and security > Site Settings > Bluetooth
4. 尝试在隐私窗口中打开网站

### Q3: 找到设备但连接失败 "GATT 连接失败"

**原因：**
- 打印机距离太远或信号弱
- 打印机电池电量过低
- 打印机需要重启
- 蓝牙驱动问题

**解决：**
1. 打印机靠近设备（1 米内）
2. 重启打印机：关闭 -> 等待 10 秒 -> 开启
3. Windows: 设置 > 蓝牙 > 打印机 > 删除设备 > 重新配对
4. 更新系统蓝牙驱动

### Q4: 连接成功但找不到服务 "找不到兼容的蓝牙服务"

**原因：**
- 打印机使用的服务 UUID 不在支持列表中
- 打印机固件版本过旧或过新

**解决：**
1. 检查打印机说明书，确认蓝牙服务 UUID
2. 如果是新的 UUID，需要更新网页代码（联系开发者）
3. 尝试在 Windows 蓝牙设置中删除并重新配对设备

### Q5: 打印开始但中途断开或打印不完整

**原因：**
- 蓝牙信号干扰
- 打印机处理速度太快（缓冲溢出）
- 蓝牙包传输延迟

**解决：**
1. 设备离打印机更近
2. 减少周围其他蓝牙/WiFi 设备
3. 按钮上显示 "📱 蓝牙打印" 时断开重新连接
4. 点击按钮后等待提示 "已连接" 再操作

---

## 实时调试（开发者）

如果您需要更详细的诊断，可以在浏览器控制台执行：

```javascript
// 检查蓝牙支持
console.log('蓝牙支持:', typeof navigator.bluetooth !== 'undefined');
console.log('安全上下文 (HTTPS/localhost):', window.isSecureContext);

// 检查用户代理
console.log('浏览器:', navigator.userAgent);

// 创建临时的 ESCPOSPrinter 实例测试（需要 React console）
// 可以在 IssuePointCard.jsx 中添加临时测试代码
```

---

## 反馈和改进

如果遇到新的错误类型或问题，请：

1. **截图错误日志**（F12 -> Console 标签）
2. **记录步骤**：什么操作触发了错误
3. **检查日志中是否有**：`[Bluetooth]` 开头的信息
4. 告诉开发者错误的 `Name` 和 `Message` 部分

这样可以快速诊断问题并改进支持。

---

**最后修改：** 2026-03-14  
**版本：** 2.0 - 改进的错误处理和多 UUID 支持
